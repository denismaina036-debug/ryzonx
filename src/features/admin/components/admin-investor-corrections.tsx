"use client";
import { useState } from "react";
import { AdminFinanceShell } from "@/features/admin/components/admin-finance-shell";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { formatCurrency } from "@/lib/utils";
type Item={id:string;amount:number;status:string;label:string}; type Investor={profile:{id:string;full_name:string|null;email:string};deposits:Item[];allocations:Item[]};
export function AdminInvestorCorrections(){const[email,setEmail]=useState("");const[data,setData]=useState<Investor|null>(null);const[reason,setReason]=useState("");const[amounts,setAmounts]=useState<Record<string,string>>({});const[message,setMessage]=useState<string|null>(null);const[busy,setBusy]=useState(false);async function call(method:"GET"|"PATCH"|"POST",body?:unknown){const r=await fetch(method==="GET"?`/api/admin/investor-corrections?email=${encodeURIComponent(email)}`:"/api/admin/investor-corrections",{method,headers:body?{"Content-Type":"application/json"}:undefined,body:body?JSON.stringify(body):undefined});const j=await r.json();if(!r.ok)throw new Error(j.error);return j}async function search(){setBusy(true);try{const j=await call("GET");setData(j.investor);setMessage(j.investor?null:"No investor found.")}catch(e){setMessage(e instanceof Error?e.message:"Search failed")}finally{setBusy(false)}}async function save(kind:"deposit"|"investment",id:string){const amount=Number(amounts[id]);if(!reason.trim()||amount<=0)return setMessage("Enter an amount and admin-only reason.");setBusy(true);try{await call("PATCH",{kind,id,amount,reason});setMessage("Correction saved; withdrawals are held pending verification.");await search()}catch(e){setMessage(e instanceof Error?e.message:"Correction failed")}finally{setBusy(false)}}async function allow(){if(!data)return;setBusy(true);try{await call("POST",{investorId:data.profile.id});setMessage("Withdrawals enabled.")}catch(e){setMessage(e instanceof Error?e.message:"Release failed")}finally{setBusy(false)}}const rows=(kind:"deposit"|"investment",items:Item[])=>items.map(x=><div key={x.id} className="grid gap-3 rounded-lg border p-4 md:grid-cols-[1fr_140px_160px]"><div><p className="font-medium">{x.label}</p><p className="text-xs text-navy-500">{x.status} · {formatCurrency(x.amount)}</p></div><input className="rounded-md border px-3 py-2" type="number" placeholder={String(x.amount)} value={amounts[x.id]??""} onChange={e=>setAmounts(v=>({...v,[x.id]:e.target.value}))}/><Button disabled={busy} onClick={()=>void save(kind,x.id)}>Save correction</Button></div>);return <AdminFinanceShell title="Investor Financial Corrections" description="Corrections are audit logged and hold withdrawals until an administrator verifies the account."><Card><CardContent className="flex gap-3 p-5"><input className="flex-1 rounded-md border px-3 py-2" value={email} onChange={e=>setEmail(e.target.value)} placeholder="investor@email.com"/><Button disabled={busy} onClick={()=>void search()}>Search</Button></CardContent></Card>{data&&<><Card><CardHeader><CardTitle>{data.profile.full_name||"Investor"} · {data.profile.email}</CardTitle></CardHeader><CardContent className="space-y-3"><textarea className="w-full rounded-md border p-3" value={reason} onChange={e=>setReason(e.target.value)} placeholder="Admin-only correction reason"/><Button variant="outline" disabled={busy} onClick={()=>void allow()}>Allow withdrawals</Button></CardContent></Card><Card><CardHeader><CardTitle>Deposits</CardTitle></CardHeader><CardContent className="space-y-3">{rows("deposit",data.deposits)}</CardContent></Card><Card><CardHeader><CardTitle>Pool investments</CardTitle></CardHeader><CardContent className="space-y-3">{rows("investment",data.allocations)}</CardContent></Card></>}{message&&<p className="text-sm">{message}</p>}</AdminFinanceShell>}
